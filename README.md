# ai-support

[![CI](https://github.com/tagashi666/ai-support/actions/workflows/ci.yml/badge.svg)](https://github.com/tagashi666/ai-support/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Node.js 22+](https://img.shields.io/badge/Node.js-22%2B-43853d.svg)](package.json)

Самостоятельно размещаемая панель поддержки. Telegram Business, обычная личка
бота и тикеты Bedolaga попадают в единый инбокс, а AI готовит ответы с опорой
на базу знаний. Любую интеграцию можно подключить позже: пустая панель
запускается только с `PANEL_TOKEN`.

Личные сообщения подключаются через официальный механизм Telegram Business —
connected business bot. MTProto и файл сессии не используются: бот получает
`business_message` и отвечает от имени подключённого аккаунта.

## Содержание

- [Возможности](#возможности)
- [Быстрый старт](#быстрый-старт)
- [Роли и работа команды](#роли-и-работа-команды)
- [Подключение каналов](#подключение-каналов)
- [AI и безопасность ответов](#ai-и-безопасность-ответов)
- [База знаний](#база-знаний)
- [Обновления](#обновления)
- [Развёртывание](#развёртывание)
- [Проверка и диагностика](#проверка-и-диагностика)
- [Архитектура](#архитектура)
- [Ограничения](#ограничения)
- [Документация](#документация)

## Возможности

- **Три канала в одном инбоксе:** Telegram Business, обычный Telegram-бот и
  тикеты Bedolaga; каналов и подключений каждого типа может быть несколько.
- **Папки как в Telegram:** один источник можно включить сразу в несколько
  настраиваемых папок, а поиск остаётся общим для всего инбокса.
- **Голый запуск:** Telegram, Bedolaga, Remnawave и AI необязательны.
- **Ответ от имени аккаунта:** оператор работает из панели, не переключаясь
  между чатами и админками.
- **Управление тикетом:** открытие и закрытие в панели меняет состояние самого
  тикета Bedolaga, а не только локальную метку.
- **Профиль Telegram:** аватар и `@username` видны в списке и внутри диалога.
- **Три режима AI:** выключен, предлагает черновик или отвечает автоматически.
- **Безопасный автоответ:** чувствительные темы, недостаточная уверенность и
  недавняя работа оператора автоматически возвращают диалог человеку.
- **Контекст клиента:** карточка из Support API и интеграции тикетов.
- **Контроль окна Telegram:** панель отслеживает разрешённые 24 часа для ответа.
- **Передача оператору:** AI предупреждает клиента и замолкает до ответа человека.
- **Уведомления:** новые обращения, ответы AI, эскалации, агрессия, передача
  человеку и просрочки приходят в Telegram.
- **Рабочие инструменты:** расширенный набор шаблонов, внутренние заметки,
  вложения, флаги серверов и статистика с медианой и p90.
- **Командная работа:** роли, назначение диалогов, индикатор присутствия,
  SLA-очередь, сохранённые фильтры и полнотекстовый поиск.
- **Единый профиль клиента:** общая карточка, теги и заметки для обращений из
  разных каналов; административные действия попадают в журнал аудита.
- **Диагностика:** состояние каналов, AI, Remnawave, уведомлений и базы знаний
  видно в одном экране без показа внутренних идентификаторов интеграций.
- **База знаний:** Markdown, внешний FAQ, полнотекстовый поиск и майнинг
  проверенных ответов из переписки, в том числе после закрытия диалога.
- **Предрелизные обновления:** панель проверяет GitHub и создаёт безопасный
  запрос на установку точного release-тега.

## Быстрый старт

### Docker

Понадобятся только Docker и Docker Compose v2. Домен, Telegram и остальные
интеграции можно добавить после запуска.

```bash
git clone https://github.com/tagashi666/ai-support.git
cd ai-support

./install.sh          # создаст .env, сгенерирует токен и сразу запустит панель
```

По умолчанию панель доступна только локально: `http://127.0.0.1:8080`. При
первом открытии она запросит токен из `PANEL_TOKEN`.

### Без Docker

Требуется Node.js 22 или новее.

```bash
cp .env.example .env
nano .env
npm ci
npm run selfcheck
npm run dev
```

Полная установка с nginx, TLS и диагностикой описана в [DEPLOY.md](DEPLOY.md).

## Роли и работа команды

`PANEL_TOKEN` остаётся аварийным доступом владельца с ролью администратора.
Остальным сотрудникам администратор создаёт отдельные токены во вкладке
**Команда**:

| Роль | Возможности |
|---|---|
| Администратор | Все диалоги, настройки, команда, обновления и аудит |
| Руководитель | Диалоги, назначения, SLA, проверка обучения AI и аудит |
| Оператор | Ответы, статусы, заметки, профиль клиента и свои фильтры |
| Наблюдатель | Только чтение диалогов без ссылок подписки и изменений |

Токен сотрудника показывается один раз при создании или ротации. В базе
хранится только SHA-256, поэтому забытый токен нельзя прочитать — его нужно
выпустить заново. Назначение действует 15 минут и продлевается активностью;
панель показывает, кто сейчас смотрит или набирает ответ в диалоге.

## Подключение каналов

### Telegram Business

1. В **@BotFather → Bot Settings → Business Mode** включите Business Mode.
2. В аккаунте поддержки откройте **Настройки → Telegram для бизнеса → Чат-боты**
   и выберите бота.
3. Выдайте только два права: **отвечать на сообщения** и **отмечать прочитанным**.
4. Сначала ограничьте список чатов своими тестовыми аккаунтами. После проверки
   расширьте доступ на клиентов.

`npm run selfcheck` показывает состояние Business Mode. Если он выключен,
обычная личка бота всё равно работает. Premium не требуется, но к одному
аккаунту можно подключить только одного бизнес-бота.

### Обычный Telegram-бот

Достаточно задать `BOT_TOKEN`. Пользователь сначала пишет боту сам, после чего
диалог появляется с каналом «Бот». Он не смешивается с Telegram Business даже
при одинаковом Telegram ID. Ответы, цитаты, вложения, аватар и username
обрабатываются тем же инбоксом.

### Несколько Telegram и Remnawave

Для нескольких ботов задайте массив подключений. Один бот принимает обычную
личку и может быть подключён к нескольким Business-аккаунтам; каждое такое
подключение автоматически становится отдельным источником:

```ini
TELEGRAM_BOTS_JSON=[{"id":"telegram:main","name":"Основная линия","token":"123:token"},{"id":"telegram:vip","name":"VIP","token":"456:token"}]
```

Несколько Remnawave-панелей работают параллельно и участвуют в поиске клиента
и сводке серверов независимо:

```ini
REMNAWAVE_PANELS_JSON=[{"id":"remnawave:eu","name":"Европа","url":"https://eu.example.com","token":"secret","readOnly":true},{"id":"remnawave:asia","name":"Азия","url":"https://asia.example.com","token":"secret","readOnly":true}]
```

Идентификаторы должны быть уникальными и постоянными: по ним сохраняется
состав папок. Папки создаёт администратор в **Настройки → Источники и папки**;
пересечения разрешены, поэтому один диалог может отображаться в нескольких
папках. Секреты интеграций в интерфейс и базу папок не попадают.

### Bedolaga

Канал включается переменными `BEDOLAGA_*`. Поллер импортирует активные тикеты
и историю, ответы отправляются через API. Кнопки состояния синхронно меняют
открытие, ожидание и закрытие удалённого тикета (`open`, `pending`, `closed`);
локальный статус меняется только после успешного ответа Bedolaga. Порядок настройки
приведён в [DEPLOY.md](DEPLOY.md).

## AI и безопасность ответов

### Режимы

| `AI_MODE` | Поведение |
|---|---|
| `off` | AI не обрабатывает диалоги |
| `suggest` | AI пишет черновик, отправляет оператор — рекомендуемый стартовый режим |
| `auto` | AI отправляет ответ самостоятельно, если его пропустили ворота |

Режим можно переопределить для отдельного диалога: явные `suggest` и `auto`
сильнее глобального режима по умолчанию. Глобальный `off` остаётся аварийным
рубильником и отключает AI во всех диалогах.

Даже в режиме `auto` ответ остаётся оператору, если:

- модель запросила человека или уверенность ниже `AI_MIN_CONFIDENCE`;
- вопрос касается возврата, двойного списания, суда или прямой просьбы об операторе;
- диалог эскалирован;
- оператор открыл диалог или недавно отвечал в нём;
- исчерпан лимит `AI_AUTO_PER_HOUR`;
- ответ не подтверждён разрешённым источником знаний.

Каждый черновик сохраняется в `ai_suggestion`. Соотношение отправленных,
отредактированных и отклонённых предложений видно в статистике и помогает
оценить готовность к режиму `auto`.

Пока карточка диалога открыта, браузер присылает heartbeat. Responder проверяет
захват до запроса к модели и после него, а Outbox — непосредственно перед
внешним API. Поэтому AI не отвечает поверх работающего оператора даже при
медленной генерации.

Подробная модель угроз, границы возможностей AI и защита панели описаны в
[SECURITY.md](SECURITY.md).

### Окно ответа Telegram

Telegram разрешает бизнес-боту отвечать в личный чат только в течение 24 часов
после последнего входящего сообщения. Обойти это ограничение нельзя.

- время последнего входящего хранится в `conversation.last_inbound_at`;
- `Outbox` блокирует отправку после закрытия окна;
- AI возвращает `skip`, не вызывая Telegram;
- композер панели предупреждает об истечении времени и затем блокируется.

Для тикетов внешней системы это окно не применяется.

## База знаний

### Источники

База синхронизируется каждые 15 минут из двух источников:

- `kb/*.md` — первый заголовок Markdown становится названием статьи;
- внешний FAQ — если включена интеграция тикетов.

Поиск работает через FTS5 в SQLite, без отдельной векторной базы и сервиса
эмбеддингов. Удалённые из источника документы исчезают и из индекса.

### Майнинг из переписки

Поддерживаются три источника:

```bash
# закрытые тикеты внешней системы
npm run kb:mine -- --archive --dry-run
npm run kb:mine -- --archive --limit 300

# экспорт Telegram Desktop в формате JSON
npm run kb:mine -- --export ~/result.json
npm run kb:mine -- --export ~/result.json --me user123456789

# накопленные панелью диалоги
npm run kb:mine          # диалоги, где предложенный ответ исправили или отклонили
npm run kb:mine -- --all # все отвеченные диалоги
```

Bot API не отдаёт прошлую историю личных чатов, поэтому для неё требуется
экспорт Telegram Desktop. Перед отправкой материала модели система удаляет
ключи, ссылки подписок, UUID, токены, карты, телефоны, IP-адреса, Telegram ID и
имена пользователей. Пары с остаточными признаками секрета отбрасываются.

Схожие обращения сначала группируются по темам, затем каждая тема превращается
в статью. Запросы режутся по токенам и учитывают лимиты провайдера. `--dry-run`
показывает темы, расход и ожидаемое время, но не запускает синтез.

Результаты сохраняются в `kb/draft/` и не участвуют в ответах до ручной проверки:

```bash
mv kb/draft/statya.md kb/
```

Если в настройках включить «обучение по закрытым диалогам», после разрешения
обращения запускается майнинг только этого диалога. Он удаляет персональные и
секретные данные и складывает результат в черновики. Самостоятельно публиковать
статьи или менять рабочую базу AI не может.

## Обновления

Экран «Настройки → Обновление» проверяет релизы репозитория и показывает кнопку
«Обновить». Нажатие запускает установленный `systemd` host-updater: он скачивает
архив и отдельный SHA-256 только из релиза `tagashi666/ai-support`, проверяет
состав архива и версию, собирает изолированный образ, делает согласованную
копию SQLite, переключает контейнер и проверяет приложение, БД, каналы и nginx.
При любой ошибке после переключения старая версия и её база восстанавливаются
автоматически. В настройках доступен и ручной откат на последнюю копию.

Приложение принципиально не получает Docker socket, root и доступ к systemd:
панель только создаёт ограниченный файл-запрос, а привилегированный исполнитель
хранит своё состояние и compose-override вне доступного контейнеру каталога.
`./install.sh`, запущенный от root, устанавливает исполнитель автоматически.
Для уже работающей установки его можно включить один раз:

```bash
sudo bash scripts/install-host-updater.sh /root/ai-support
```

## Развёртывание

### Ручное обновление контейнера

```bash
docker compose up -d --build
```

Порт `8080` публикуется только на `127.0.0.1`: открытый `8080:8080` обошёл бы
правила UFW. Внешний HTTPS-доступ должен предоставлять реверс-прокси.

### nginx и TLS

Готовый конфиг находится в [`deploy/ai-support.conf`](deploy/ai-support.conf).
Он завершает TLS, передаёт WebSocket и корректный `X-Forwarded-For`.

```bash
apt install -y nginx certbot python3-certbot-nginx

cp deploy/ai-support.conf /etc/nginx/sites-available/ai-support.conf
sed -i 's/support\.example\.com/support.вашдомен.ru/g' \
  /etc/nginx/sites-available/ai-support.conf
ln -s /etc/nginx/sites-available/ai-support.conf /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default

certbot --nginx -d support.вашдомен.ru
nginx -t && systemctl reload nginx
ufw allow 80,443/tcp
```

После настройки укажите `PANEL_URL=https://support.вашдомен.ru`, чтобы ссылки
из уведомлений открывали нужный диалог. DNS, автоматическое продление
сертификата и вариант с Caddy разобраны в [DEPLOY.md](DEPLOY.md).

## Проверка и диагностика

### Проверки проекта

```bash
npm run typecheck  # проверка типов
npm test           # тесты без сети и настоящих токенов
npm run selfcheck  # проверка конфигурации текущего сервера
```

| Набор | Что проверяет |
|---|---|
| `uitest.ts` | структура и инварианты интерфейса |
| `smoke.ts` | Business, личка бота, дедупликация, окно ответа и защита AI |
| `webtest.ts` | API панели, роли, отсутствие утечек и WebSocket |
| `baretest.ts` | запуск без Telegram, Bedolaga, Remnawave и AI |
| `aitest.ts` | AI-ворота, модель, FTS, поллер и регрессии |
| `minetest.ts` | экспорт Telegram, архив тикетов и группировка тем |
| `notifytest.ts` | уведомления, ограничения частоты и ошибки доставки |
| `sectest.ts` | фильтр исходящего, билеты, лимиты и read-only операции |

### Клиент не находится в Remnawave

```bash
docker compose run --rm ai-support node dist/cli/rwprobe.js 123456789 example_user
```

Команда показывает ответы точечных методов, построение индекса и найденные
совпадения. Та же диагностика доступна в панели по кнопке «Почему не нашёлся».

Поиск учитывает `telegramId`, служебное имя `user_{telegram_id}_*`, Telegram ID
и `@username` в описании, а также short UUID ссылки подписки. Поскольку параметр
`search` некоторых версий панели ненадёжен, индекс загружается постранично и
проверяется локально.

### Ошибки прав доступа

Если панель не может сохранить статью или базу:

```bash
chown -R 1000:1000 kb data
docker compose restart
```

Контейнер работает под UID `1000`. `install.sh` выставляет владельца каталогов,
а `npm run selfcheck` проверяет возможность записи до запуска.

## Архитектура

```text
Telegram Business ─┐                         ┌── Telegram Business API
Telegram Bot DM ───┼──→ Store ──→ Outbox ───┼── Telegram Bot API
Bedolaga tickets ──┘      │                  └── Bedolaga API
                          ├── панель (HTTP + WS)
                          └── AI + SLA + майнинг знаний
```

`Outbox` — единственная точка исходящей отправки. Панель, AI и автоматика не
вызывают Telegram или API тикетов напрямую, поэтому не обходят окно ответа и
всегда записывают исходящее сообщение в историю.

AI подписан на события хранилища и одинаково работает для всех каналов.
Миграции только добавляются в конец `MIGRATIONS`: уже применённые миграции
нельзя изменять задним числом.

Подробная раскладка модулей и инварианты находятся в
[ARCHITECTURE.md](ARCHITECTURE.md).

## Ограничения

- Bot API не загружает старую личную переписку: панель начинает собирать её
  только после запуска.
- Ответ и изменение состояния тикета зависят от совместимости API Bedolaga;
  входящие вложения при этом доступны.
- API ответа в тикет не идемпотентен. После сетевого обрыва нужно проверить
  историю перед повторной отправкой.
- К аккаунту Telegram можно подключить только одного бизнес-бота.
- Telegram помечает ответы флагом `via_business_connection`.
- Билет чтения WebSocket и вложений действует десять минут и временно виден в
  URL. Он не разрешает изменения, но access-логи следует считать чувствительными.
- `PANEL_TOKEN` всегда имеет права администратора; токены сотрудников нельзя
  восстановить из базы, только перевыпустить.
- Обновление по кнопке требует однажды установленного host-updater; веб-контейнер
  намеренно не управляет Docker сам. Статус проверяется командой
  `systemctl status ai-support-updater.path`.

## Документация

- [DEPLOY.md](DEPLOY.md) — установка, nginx, TLS, интеграции и обслуживание.
- [PROVIDERS.md](PROVIDERS.md) — AI-провайдеры, модели и параметры.
- [SECURITY.md](SECURITY.md) — модель угроз и границы доступа AI.
- [ARCHITECTURE.md](ARCHITECTURE.md) — слои, потоки данных и инварианты.
- [CONTRIBUTING.md](CONTRIBUTING.md) — локальная разработка и pull request.
- [CHANGELOG.md](CHANGELOG.md) — история версий.

## Лицензия

Проект распространяется по лицензии [MIT](LICENSE).
